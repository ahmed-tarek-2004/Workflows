name: Testing Env

on : 
    workflow_dispatch:


jobs:
   

   first_job:
    runs-on: ubuntu-latest
    
    steps:
    - name: Set API Key in Environment
      run: echo "PASS=12345" >> $GITHUB_ENV

    - name : mask pass 
      run: |
        echo "::add-mask::$PASS"
        echo "The pass is $PASS" 
